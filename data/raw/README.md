# Place your source PDF documents here.
# Files committed to this directory are used by GitHub Actions CI to:
#   1. Build the ChromaDB vector store (ci_ingest.py)
#   2. Run the RAGAS evaluation gate (ci_eval_gate.py)
#
# Currently tracked PDFs:
#   (add your PDF filenames here as a record)
#
# NOTE: Keep this directory focused — only commit PDFs that relate to
# the frozen testset questions in tests/frozen_testset.csv.
